fastapi==0.115.0
uvicorn[standard]==0.30.0
sqlalchemy==2.0.35
jinja2==3.1.4
httpx==0.27.0
beautifulsoup4==4.12.3
apscheduler==3.10.4
stripe==10.5.0
# Already a hard dependency of stripe; pinned because app.main names
# stripe.RequestsClient directly to bound the checkout call's timeout.
requests==2.34.2
python-multipart==0.0.12
python-dotenv==1.0.1
aiosmtplib==3.0.2
passlib[bcrypt]==1.7.4
# passlib 1.7.4's bcrypt backend fails to load against bcrypt>=5 — keep it pinned.
bcrypt==4.2.1
itsdangerous==2.2.0
# Test-only dependencies (pytest) live in requirements-dev.txt so the deploy
# never installs them.
